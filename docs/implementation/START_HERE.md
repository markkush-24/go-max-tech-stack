# Start Here

1. Add the entire `docs/audit/` and `docs/implementation/` directories to the real project repository.
2. Open `TASK_INDEX.md`.
3. Start with `TASK-001`; do not jump to observability implementation before the P0/P1 blockers required by its dependencies are resolved.
4. Give Codex `AGENT_EXECUTION_PROTOCOL.md` once per chat and one task card at a time using `CODEX_PROMPT_TEMPLATE.md`.
5. Review the diff with `REVIEW_CHECKLIST.md`.
6. Mark the task `DONE` in `TASK_INDEX.md` only after acceptance and append `COMPLETION_LOG.md`.
7. Unlock tasks whose dependencies are now complete.
8. Request an independent checkpoint re-audit after P1, after P2/P3, after P4/P5, after P6 and after P7.

The first executable task is:

`docs/implementation/tasks/TASK-001-repo-track-quality-assets.md`
